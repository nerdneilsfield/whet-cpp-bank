---
qid: cpp-fp-005
type: single
kp: [cpp-function-pointers]
difficulty: easy
answer_key: C
---

# 认知：成员函数指针的特殊性

关于成员函数指针，下列说法正确的是？

A. 成员函数指针与普通函数指针语法完全相同  
B. 成员函数指针可以直接调用，无需提供对象  
C. 调用成员函数指针必须通过 `.*` 或 `->*` 运算符并提供对象实例  
D. 成员函数指针可以存入 `void*` 后再转回调用  
