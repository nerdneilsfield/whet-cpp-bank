---
qid: cpp-fp-005
type: single
kp: [cpp-function-pointers]
difficulty: easy
answer_key: C
---

# 认知：成员函数指针的特殊性

关于成员函数指针，下列说法正确的是？

A. 成员函数指针与普通函数指针语法完全相同  
B. 成员函数指针可以直接调用，无需提供对象  
C. 调用成员函数指针必须通过 `.*` 或 `->*` 运算符并提供对象实例  
D. 成员函数指针可以存入 `void*` 后再转回调用

## 解析

正确答案是 C，对应“调用成员函数指针必须通过 . 或 -> 运算符并提供对象实例”。本题考查 iostream/文件流/格式化输出的状态与缓冲规则，关键是区分格式控制、流状态和实际写入时机。其他选项容易混淆相关概念：A 项“成员函数指针与普通函数指针语法完全相同”不满足题干要求；B 项“成员函数指针可以直接调用，无需提供对象”不满足题干要求；D 项“成员函数指针可以存入 void 后再转回调用”不满足题干要求。常见误区是只凭关键字判断，而没有结合完整声明、表达式求值结果或 C++ 语义限制。
