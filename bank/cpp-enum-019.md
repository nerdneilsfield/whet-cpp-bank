---
qid: cpp-enum-019
type: multi
kp: [cpp-enum]
difficulty: easy
answer_key: [A, B, D]
---
关于传统 `enum`（非 scoped），以下哪些说法正确？（多选）

A. 枚举器直接注入包围的命名空间，可不加前缀使用
B. 枚举器可隐式转换为整数类型
C. 不能在同一命名空间中定义同名枚举器的两个不同 `enum`
D. 若不手动指定，后续枚举器的值等于前一个枚举器的值加 1
