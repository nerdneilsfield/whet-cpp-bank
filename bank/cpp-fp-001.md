---
qid: cpp-fp-001
type: single
kp: [cpp-function-pointers]
difficulty: easy
answer_key: C
---

# 认知：函数指针声明语法

下列哪个声明正确地定义了一个"指向接受两个 `int` 参数、返回 `int` 的函数"的指针 `fp`？

A. `int fp(int, int)*;`  
B. `int* fp(int, int);`  
C. `int (*fp)(int, int);`  
D. `(*int) fp(int, int);`  
