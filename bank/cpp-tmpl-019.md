---
qid: cpp-tmpl-019
type: multi
kp: [cpp-templates]
difficulty: easy
answer_key: [A, C]
---

# 模板特化适用场景

下列哪些场景适合使用模板完整特化？（多选）

A. 为 `const char*` 类型的比较函数提供按字符串内容比较的特殊实现
B. 需要让同一模板支持可变数量的类型参数
C. `std::vector<bool>` 使用位压缩存储，与通用 `vector<T>` 行为不同
D. 希望模板在编译期计算斐波那契数列
E. 为所有整数类型统一提供一种实现
