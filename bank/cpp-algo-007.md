---
qid: cpp-algo-007
type: fill
kp: [cpp-algorithms]
difficulty: easy
answer_key: "end(v)"
---

```cpp
#include <algorithm>
#include <vector>
std::vector<int> v = {1, 3, 5, 7};
auto it = std::find(v.begin(), v.end(), 4);
```

执行后，`it` 的值等于 `______`（用 `v` 的成员函数表达式填写）。
