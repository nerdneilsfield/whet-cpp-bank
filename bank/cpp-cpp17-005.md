---
qid: cpp-cpp17-005
type: single
kp: [cpp-cpp14-17]
difficulty: easy
answer_key: B
---

# 认知：[[nodiscard]] 属性的作用

`[[nodiscard]]` 属性标记在函数上时，其作用是什么？

A. 强制调用方必须检查返回值，否则编译报错
B. 若调用方忽略返回值，编译器发出警告
C. 禁止该函数被内联优化
D. 标记该函数的返回值不可修改
