---
qid: cpp-types-fill-med-001
type: fill
kp: [cpp-types]
difficulty: medium
answer_key: "8"
---
C++ 标准保证 `char` 至少有 ___ 位。

---

**解析：**

C++ 标准规定 `char` 的大小为 1 字节，而字节的最小宽度由 `<climits>` 中的 `CHAR_BIT` 定义，在几乎所有平台上此值为 8。但标准仅要求 `CHAR_BIT >= 8`，因此最准确的说法是 char 至少 8 位。这一保证是类型系统的基础前提。

## 解析

填入 `8`，因为题目要求的是该规则下的精确表达或标准名称。大小相关题要分清 C++ 标准保证、平台 ABI 约定和对象表示；`sizeof` 结果以字节计，类型为 `size_t`。 常见误区是写出近似说法、旧式写法或省略必要的命名空间/符号，导致与答案含义不完全一致。
