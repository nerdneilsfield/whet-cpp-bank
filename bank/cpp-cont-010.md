---
qid: cpp-cont-010
type: fill
kp: [cpp-containers]
difficulty: easy
answer_key: FIFO（先进先出）
---

`std::queue` 遵循 `______` 语义：第一个入队的元素第一个被取出，通过 `front()` 访问队首元素，通过 `back()` 访问队尾元素。

## 解析

应填 `FIFO（先进先出）`。`std::queue` 是先进先出适配器，队首由 `front()` 访问。 这个答案对应题干要求的标准名称、成员函数或复杂度写法。常见误区是填写相近但语义不同的接口，或漏掉作用域、大小写、下划线等精确拼写要求。 进一步判断时要回到题干给出的具体代码、接口名和标准语义，避免用相似概念的经验结论替代本题要求。
