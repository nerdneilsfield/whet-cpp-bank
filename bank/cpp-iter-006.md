---
qid: cpp-iter-006
type: single
kp: [cpp-iterators]
difficulty: easy
answer_key: B
---

`cbegin()` / `cend()` 返回的迭代器与 `begin()` / `end()` 的主要区别是？

A. 指向容器末尾的下一个元素
B. 返回 `const` 迭代器，不允许通过它修改元素
C. 只能在 `const` 容器上调用
D. 遍历顺序相反
