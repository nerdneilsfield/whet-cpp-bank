---
qid: cpp-types-024
type: fill
kp: [cpp-types]
difficulty: easy
answer_key: "nullptr"
---
C++11 推荐用来表示空指针的字面量是：___（而非旧式的 `NULL` 或 `0`）

## Explanation

填入 `nullptr`，因为题目要求的是该规则下的精确表达或标准名称。指针、数组和引用题要注意数组到指针退化、指针算术按元素大小推进，以及引用绑定后只是对象别名。 常见误区是写出近似说法、旧式写法或省略必要的命名空间/符号，导致与答案含义不完全一致。
