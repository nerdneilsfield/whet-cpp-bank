---
qid: cpp-func-009
type: single
kp: [cpp-functions]
difficulty: easy
answer_key: B
---

以下两个函数是否构成合法的重载？

```cpp
int compute(int x);
double compute(int x);
```

A. 是，返回类型不同即可
B. 否，仅返回类型不同不能区分重载
C. 是，只要函数名相同即可
D. 否，函数名必须不同
