---
qid: cpp-cast-005
type: single
kp: [cpp-casting]
difficulty: easy
answer_key: B
---

# dynamic_cast 前提条件

使用 `dynamic_cast` 的必要条件是？

A. 基类有 `virtual` 析构函数，且继承为 private
B. 基类至少有一个 `virtual` 函数（即多态类型）
C. 编译时开启 `-O2` 优化
D. 派生类重写了基类的所有虚函数
