---
qid: cpp-build-020
type: single
kp: [cpp-build-tools]
difficulty: easy
answer_key: A
---

# 执行 `make` 命令时，若不指定目标，默认执行哪个目标？

A. Makefile 中第一个出现的目标  
B. 名为 `all` 的目标（必须存在）  
C. 名为 `default` 的目标（必须存在）  
D. 最后一个出现的目标
