---
qid: cpp-stl-014
type: fill
kp: [cpp-stl-basics]
difficulty: easy
answer_key: "stoi"
---

将 `std::string` 转换为 `int`，应使用标准库函数 `std::_____()` 。

## 解析

填入 `stoi`，因为题目要求的是该规则下的精确表达或标准名称。STL 题要关注容器语义、迭代器失效、复杂度和算法是否修改原序列；不要把所有容器都按数组模型理解。 常见误区是写出近似说法、旧式写法或省略必要的命名空间/符号，导致与答案含义不完全一致。
