---
qid: cpp-cpp17-003
type: single
kp: [cpp-cpp14-17]
difficulty: easy
answer_key: A
---

# 认知：std::optional 的含义

`std::optional<int>` 表示什么？

A. 一个可能持有 `int` 值、也可能为空（`nullopt`）的对象
B. 一个指向 `int` 的可空指针
C. 一个只读的 `int` 包装器
D. 一个线程安全的 `int` 容器
