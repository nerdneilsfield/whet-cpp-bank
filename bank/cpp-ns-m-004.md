---
qid: cpp-ns-m-004
type: multi
kp: [cpp-preprocessor]
difficulty: easy
answer_key: [A, B, D]
---

下列哪些是**合法的 C++ 预处理指令**？（选择所有正确项）

A. `#include <vector>`
B. `#if defined(DEBUG) && !defined(NDEBUG)`
C. `#namespace std`
D. `#error "不支持该平台"`

