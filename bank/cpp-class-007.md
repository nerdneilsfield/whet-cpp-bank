---
qid: cpp-class-007
type: single
kp: [cpp-classes]
difficulty: easy
answer_key: D
---

`this` 指针在 C++ 类中的含义是？

A. 指向当前类的父类对象
B. 指向类的静态成员
C. 指向类的成员函数
D. 指向调用该成员函数的当前对象本身

## Explanation

正确答案为 D，因为“指向调用该成员函数的当前对象本身”符合题干所问的 C++ 规则或代码执行结果。A、B、C 项分别混淆了相近概念、错误语法或不会发生的执行路径。常见误区是凭直觉看关键字，而没有按标准规则和代码顺序逐步判断。类与对象题要区分默认访问权限、对象生命周期、静态成员、const 成员函数和初始化规则。
