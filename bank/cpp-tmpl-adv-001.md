---
qid: cpp-tmpl-adv-001
type: single
kp: [cpp-templates]
difficulty: easy
answer_key: A
---

`std::is_same<int, int>::value` 和 `std::is_same<int, float>::value` 的值分别是？

A. `true` 和 `false`
B. `false` 和 `true`
C. `true` 和 `true`
D. `false` 和 `false`
