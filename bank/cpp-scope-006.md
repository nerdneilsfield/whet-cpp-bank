---
qid: cpp-scope-006
type: single
kp: [cpp-scope-linkage]
difficulty: easy
answer_key: C
---

# mutable 关键字用途

`mutable` 关键字的主要用途是什么？

A. 允许在运行时修改常量表达式
B. 声明一个可以跨函数调用保持值的局部变量
C. 允许 `const` 成员函数修改被 `mutable` 修饰的成员变量
D. 将成员变量标记为线程安全可修改
