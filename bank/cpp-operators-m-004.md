---
qid: cpp-operators-m-004
type: multi
kp: [cpp-operators]
difficulty: easy
answer_key: [A, C]
---

关于 C++ 中 `++` 运算符，下列哪些说法正确？选择所有正确项。

A. 前置 `++i` 先自增，再将新值作为表达式结果返回。
B. 后置 `i++` 与前置 `++i` 在单独成语句时（如 `i++;`）产生不同的副作用。
C. 后置 `i++` 的返回值是自增前的原始值。
D. 对于内置整型，前置 `++i` 比后置 `i++` 的运行效率更低。
