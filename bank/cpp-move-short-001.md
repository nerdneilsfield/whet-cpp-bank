---
qid: cpp-move-short-001
type: short
kp: [cpp-move-semantics]
difficulty: medium
rubric:
  - 指出 move 操作针对右值或显式 std::move 转换出的右值引用
  - 给出几种典型触发场景（返回临时对象/std::move/函数按值返回的命名返回值）
  - 指出 NRVO（具名返回值优化）可能直接省略 move
  - 提到只有定义了移动构造或移动赋值的类型才能真正"被 move"
---
请说明在 C++ 中什么情况下会触发对象的"移动"（move）操作。

---

**参考答案：**

移动构造或移动赋值在源对象是右值时被选用：典型场景包括返回临时对象（rvalue）、显式调用 `std::move(x)` 将左值转换为右值、按值传参/返回值时编译器视具体规则可能选择 move。函数按值返回局部变量时，编译器优先尝试 NRVO/拷贝消除，若无法消除则隐式 move。对没有定义移动构造/赋值的类型，move 操作会回退到拷贝。因此真正想"被 move"的类，必须提供（或不阻止编译器生成）移动特殊成员函数。

## Explanation

对象的移动通常在源表达式是右值或 xvalue 时发生，例如传入临时对象、显式写 `std::move(x)`、或返回值无法被拷贝消除时。真正是否移动还取决于目标类型是否有可用的移动构造或移动赋值。常见误区是认为编译器会因变量之后不再使用就自动移动；具名变量默认仍是左值。
