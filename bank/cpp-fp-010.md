---
qid: cpp-fp-010
type: fill
kp: [cpp-function-pointers]
difficulty: easy
answer_key: "int (*fp)(int, int)"
---

# 代码填空：声明函数指针变量

补全下列声明，使 `fp` 成为指向"接受两个 `int`，返回 `int`"的函数指针：

```cpp
________ = nullptr;
```

（只填写 `=` 左侧的声明部分，不含分号）

## 解析

应填写 `int (*fp)(int, int)`。本题考查 iostream/文件流/格式化输出的状态与缓冲规则，关键是区分格式控制、流状态和实际写入时机。该答案能与题干中的代码上下文、声明形式或输出结果精确匹配，因此既要关注语法位置，也要关注类型和值的含义。常见误区是写出近似形式但改变了指针/引用/捕获/格式化等语义，导致编译失败或结果不同。
