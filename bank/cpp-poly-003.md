---
qid: cpp-poly-003
type: single
kp: [cpp-polymorphism]
difficulty: easy
answer_key: A
---

# 含有纯虚函数的类称为______，以下说法正确的是？

A. 抽象类，不能直接实例化，但可以声明指向它的指针或引用  
B. 抽象类，既不能实例化也不能有指针  
C. 接口类，可以直接实例化  
D. 虚基类，必须通过 `virtual` 继承
