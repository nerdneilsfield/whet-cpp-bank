---
qid: cpp-io-014
type: fill
kp: [cpp-io]
difficulty: easy
answer_key: "  42"
---

# 代码阅读：setw 对齐

```cpp
#include <iomanip>
std::cout << std::setw(4) << 42;
```

程序输出是 ______（用下划线表示空格，如 `__42`，实际空格字符）。

> 提示：`setw(4)` 表示最小宽度 4，右对齐，不足补空格。

答案（原始字符）：______
