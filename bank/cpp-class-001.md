---
qid: cpp-class-001
type: single
kp: [cpp-classes]
difficulty: easy
answer_key: B
---

`class` 和 `struct` 最本质的区别是什么？

A. `struct` 不能有成员函数，`class` 可以
B. `class` 默认访问权限是 `private`，`struct` 默认是 `public`
C. `class` 不能继承，`struct` 可以
D. `struct` 不能有构造函数，`class` 可以
