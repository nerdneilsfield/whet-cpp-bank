---
qid: cpp-cpp11-003
type: single
kp: [cpp-cpp11]
difficulty: easy
answer_key: D
---

# 认知：auto 推导规则

下列关于 `auto` 的说法，**正确**的是哪一条？

A. `auto` 变量必须在函数返回类型处使用
B. `auto` 不能推导引用类型
C. `auto x = 1;` 推导出 `x` 为 `long`
D. `auto x = 1;` 推导出 `x` 为 `int`
