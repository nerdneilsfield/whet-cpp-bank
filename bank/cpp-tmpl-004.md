---
qid: cpp-tmpl-004
type: single
kp: [cpp-templates]
difficulty: easy
answer_key: D
---

# 模板定义位置

关于模板的定义位置，下列说法正确的是？

A. 模板的声明放在头文件，定义放在 `.cpp` 文件，与普通函数一样
B. 模板只能定义在 `.cpp` 文件中
C. 模板必须定义在 `main()` 函数之前
D. 模板的定义必须在头文件中（或调用处可见的位置），不能分离到单独的 `.cpp` 文件编译
