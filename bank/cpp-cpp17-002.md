---
qid: cpp-cpp17-002
type: single
kp: [cpp-cpp14-17]
difficulty: easy
answer_key: C
---

# 认知：if constexpr 的用途

关于 `if constexpr`，下列说法正确的是？

A. 运行时根据条件选择分支，和普通 `if` 完全相同
B. 只能用于模板特化，不能用于普通函数
C. 在编译期对条件求值，未选中的分支不会被实例化
D. 等价于 `#ifdef` 预处理指令
