---
qid: cpp-ns-018
type: single
kp: [cpp-preprocessor]
difficulty: easy
answer_key: B
---

要在 `file_a.cpp` 中访问 `file_b.cpp` 定义的全局变量 `int counter`，应在 `file_a.cpp` 中写：

A. `static int counter;`
B. `extern int counter;`
C. `#include "file_b.cpp"`
D. `global int counter;`
