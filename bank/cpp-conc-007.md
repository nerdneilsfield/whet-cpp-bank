---
qid: cpp-conc-007
type: single
kp: [cpp-concurrency]
difficulty: easy
answer_key: D
---

# 认知：`std::atomic` 的用途

使用 `std::atomic<int>` 的主要目的是？

A. 提升变量的访问速度  
B. 防止变量被编译器优化掉  
C. 让变量在多个翻译单元中可见  
D. 保证对变量的读写操作是原子的，无需额外加锁  
