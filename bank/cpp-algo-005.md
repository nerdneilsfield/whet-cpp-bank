---
qid: cpp-algo-005
type: single
kp: [cpp-algorithms]
difficulty: easy
answer_key: B
---

`std::binary_search` 使用前对目标范围有什么要求？

A. 范围内元素必须唯一（无重复）
B. 范围必须已按升序（或指定比较器定义的顺序）排好序
C. 范围必须是 `std::vector`，不能是其他容器
D. 范围大小必须是 2 的幂
