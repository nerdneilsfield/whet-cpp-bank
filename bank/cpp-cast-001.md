---
qid: cpp-cast-001
type: single
kp: [cpp-casting]
difficulty: easy
answer_key: B
---

# static_cast 用途

下列哪种场景最适合使用 `static_cast`？

A. 将 `const int*` 转为 `int*`
B. 将 `double` 转为 `int`
C. 将基类指针安全地转为派生类指针（有虚函数）
D. 将 `int*` 重新解释为 `char*`
