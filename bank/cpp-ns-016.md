---
qid: cpp-ns-016
type: single
kp: [cpp-preprocessor]
difficulty: easy
answer_key: A
---

匿名命名空间（`namespace { ... }`）的主要作用是什么？

A. 将其中的符号限制在当前编译单元（`.cpp` 文件）内，相当于 `static` 的命名空间版本
B. 允许其中的符号在任意文件中被直接访问，无需限定符
C. 防止头文件被重复包含
D. 为符号提供默认的 `std` 前缀
