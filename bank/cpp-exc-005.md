---
qid: cpp-exc-005
type: fill
kp: [cpp-exceptions]
difficulty: easy
answer_key: "throw;"
---

# C++ 异常处理：重新抛出异常

在 `catch` 块中，不带任何操作数地重新抛出当前异常的语句是 ______。

## 解析

应填 `throw;`。异常题的核心是匹配规则、栈展开、`noexcept` 与异常安全保证。 这个答案对应题干要求的标准名称、成员函数或复杂度写法。常见误区是填写相近但语义不同的接口，或漏掉作用域、大小写、下划线等精确拼写要求。 进一步判断时要回到题干给出的具体代码、接口名和标准语义，避免用相似概念的经验结论替代本题要求。
