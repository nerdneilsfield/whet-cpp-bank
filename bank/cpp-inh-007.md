---
qid: cpp-inh-007
type: single
kp: [cpp-inheritance]
difficulty: easy
answer_key: C
---

下列关于 `using Base::foo` 的说法，哪项正确？

A. 将基类的 `foo` 函数重写为虚函数
B. 删除派生类中同名的 `foo`
C. 将被派生类隐藏的基类 `foo` 重新引入派生类的作用域
D. 只能用于引入 `public` 成员
