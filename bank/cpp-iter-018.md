---
qid: cpp-iter-018
type: multi
kp: [cpp-iterators]
difficulty: easy
answer_key: [A, C, D]
---

以下哪些操作是**随机访问迭代器**支持的？（多选）

A. `it[n]`（下标访问）
B. `it->field`（成员访问，前向迭代器也支持）
C. `it1 - it2`（迭代器相减得距离）
D. `it1 < it2`（大小比较）
E. `++it`（自增，前向迭代器也支持）
