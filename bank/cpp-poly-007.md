---
qid: cpp-poly-007
type: single
kp: [cpp-polymorphism]
difficulty: easy
answer_key: A
---

# 关于构造函数与 `virtual`，以下哪项是正确的？

A. 构造函数不能声明为 `virtual`  
B. 构造函数可以声明为 `virtual`，效果与普通 `virtual` 函数相同  
C. 只有默认构造函数不能是 `virtual`，其他构造函数可以  
D. `virtual` 构造函数用于工厂模式，是标准 C++ 特性
