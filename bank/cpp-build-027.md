---
qid: cpp-build-027
type: single
kp: [cpp-build-tools]
difficulty: easy
answer_key: A
---

# 执行 `make -j4` 的效果是什么？

A. 最多同时运行 4 个编译任务（并行编译）  
B. 只编译前 4 个目标  
C. 将编译日志限制在 4 行  
D. 将优化级别设为 `-O4`

## 解析

正确答案为 A，因为“最多同时运行 4 个编译任务（并行编译）”符合题干所问的 C++ 规则或代码执行结果。B、C、D 项分别混淆了相近概念、错误语法或不会发生的执行路径。常见误区是凭直觉看关键字，而没有按标准规则和代码顺序逐步判断。构建题要区分预处理、编译、汇编、链接以及编译选项、库路径和目标文件的职责。
