---
qid: cpp-types-m-004
type: multi
kp: [cpp-types]
difficulty: easy
answer_key: [A, B, C]
---
以下哪些类型可以由 `auto` 正确推导？（选择所有正确项）

A. `auto x = 42;`（推导为 `int`）
B. `auto y = 3.14;`（推导为 `double`）
C. `auto z = 'A';`（推导为 `char`）
D. `auto w;`（不初始化，推导为 `void`）
