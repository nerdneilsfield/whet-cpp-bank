---
qid: cpp-poly-004
type: single
kp: [cpp-polymorphism]
difficulty: easy
answer_key: D
---

# 关于 `override` 说明符，哪项描述正确？

A. `override` 是必须写的关键字，否则无法覆盖基类虚函数  
B. `override` 会改变函数的调用方式，使其变为静态绑定  
C. `override` 只能用于 `final` 类的成员  
D. `override` 是可选的说明符；若函数签名与基类虚函数不匹配，编译器会报错
