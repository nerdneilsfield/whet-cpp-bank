---
qid: cpp-cf-024
type: single
kp: [cpp-control-flow]
difficulty: easy
answer_key: D
---

关于 `goto` 语句，下列说法最准确的是？

A. `goto` 在 C++ 中已被删除，无法使用
B. `goto` 只能向前跳转，不能向后跳转
C. `goto` 是跳出多层嵌套循环的推荐方式
D. `goto` 会破坏代码结构化，降低可读性，现代 C++ 中极少推荐使用
