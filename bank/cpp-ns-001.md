---
qid: cpp-ns-001
type: single
kp: [cpp-preprocessor]
difficulty: easy
answer_key: B
---

`#include <vector>` 与 `#include "myfile.h"` 的主要区别是什么？

A. 前者只能包含 C 标准库，后者只能包含 C++ 标准库
B. 前者优先搜索系统/标准库目录，后者优先搜索当前文件所在目录
C. 前者在编译时展开，后者在链接时展开
D. 两者完全等价，只是风格不同
