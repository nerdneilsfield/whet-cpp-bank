---
qid: cpp-poly-001
type: single
kp: [cpp-polymorphism]
difficulty: easy
answer_key: B
---

# 关于 `virtual` 函数的描述，哪项正确？

A. `virtual` 函数只能在类的 public 区声明  
B. 通过基类指针调用 `virtual` 函数时，实际执行的是所指对象的派生类实现（动态分派）  
C. `virtual` 函数不能有函数体  
D. `virtual` 关键字在派生类重写时必须再次写出
