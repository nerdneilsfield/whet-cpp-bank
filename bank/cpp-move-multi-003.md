---
qid: cpp-move-multi-003
type: multi
kp: [cpp-move-semantics]
difficulty: medium
answer_key: [A, C, D]
---
关于右值引用的性质，**哪些说法是正确的**？（多选）

A. 一个具名的右值引用变量本身是左值表达式。
B. 右值引用不能延长临时对象的生命周期。
C. `T&&` 在模板参数推导中表现为「转发引用」，可绑定左值或右值。
D. 右值引用可重载，使得函数能区分「正在被移动」与「正在被拷贝」。

---

**解析：**

A 正确：右值引用变量在使用时是左值（具名即左值）。
B 错误：右值引用与 const 左值引用一样能延长绑定到它的临时对象生命周期。
C 正确：在模板上下文中 `T&&` 是转发引用（即 universal reference）。
D 正确：通过对 `T&` 与 `T&&` 重载可区分拷贝与移动语义。

## Explanation

正确答案是 A、C、D。具名右值引用变量作为表达式是左值；模板中的 `T&&` 在满足推导条件时是转发引用；`T&`/`T&&` 重载可区分拷贝和移动语义。B 错在右值引用绑定临时对象时可以延长其生命周期。 这类题的关键是区分语法形式、对象生命周期和所有权语义：语法看似相近时，应先判断谁拥有资源、表达式值类别是什么，以及标准是否保证该行为；不要根据实现习惯或表面写法推断答案。
