---
qid: cpp-scope-003
type: single
kp: [cpp-scope-linkage]
difficulty: easy
answer_key: A
---

# extern 声明的作用

`extern int x;` 这条声明的作用是什么？

A. 声明 `x` 定义在其他编译单元，本单元可使用该变量
B. 定义一个具有外部链接的新变量 `x`
C. 将 `x` 限制为只在本文件内可见
D. 将 `x` 声明为常量
