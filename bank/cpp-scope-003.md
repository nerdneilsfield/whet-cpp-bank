---
qid: cpp-scope-003
type: single
kp: [cpp-scope-linkage]
difficulty: easy
answer_key: A
---

# extern 声明的作用

`extern int x;` 这条声明的作用是什么？

A. 声明 `x` 定义在其他编译单元，本单元可使用该变量
B. 定义一个具有外部链接的新变量 `x`
C. 将 `x` 限制为只在本文件内可见
D. 将 `x` 声明为常量

## 解析

正确答案是 A，因为该选项对应 `声明 `x` 定义在其他编译单元，本单元可使用该变量`，符合题中代码或概念的 C++ 语义。作用域与链接性题要区分名字可见范围、对象生存期和跨翻译单元链接属性；三者相关但不是同一个概念。常见误区是把局部 static 的静态存储期理解成全局可见，或把遮蔽变量误认为修改了外层变量。 B 项、C 项、D 项 的问题在于忽略了上述规则中的一个关键条件，因此会得到看似合理但错误的结论。
