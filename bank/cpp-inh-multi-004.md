---
qid: cpp-inh-multi-004
type: fill
kp: [cpp-inheritance]
difficulty: easy
answer_key: c.A::show()
---

`A` 和 `B` 都定义了成员函数 `show()`，`C` 多重继承自 `A` 和 `B`。若要通过 `C` 的对象 `c` 显式调用 `A` 的 `show()`，正确写法是：

`________`
