---
qid: cpp-cf-020
type: single
kp: [cpp-control-flow]
difficulty: easy
answer_key: A
---

关于逻辑与运算符 `&&` 的短路求值，下列说法正确的是？

A. 若左操作数为 `false`，右操作数不会被求值
B. 两个操作数总是都被求值
C. 只有右操作数为 `false` 时才短路
D. 短路求值仅适用于 `||`，不适用于 `&&`
