---
qid: cpp-ptr-011
type: fill
kp: [cpp-arrays-pointers]
difficulty: easy
answer_key: "int& r = x"
---

声明一个绑定到 `int` 变量 `x` 的引用 `r` 的语句是 ___。

## Explanation

填入 `int& r = x`，因为题目要求的是该规则下的精确表达或标准名称。指针、数组和引用题要注意数组到指针退化、指针算术按元素大小推进，以及引用绑定后只是对象别名。 常见误区是写出近似说法、旧式写法或省略必要的命名空间/符号，导致与答案含义不完全一致。
