---
qid: cpp-cf-m-006
type: multi
kp: [cpp-control-flow]
difficulty: easy
answer_key: [A, B, C]
---

`for` 循环三段式 `for (初始化; 条件; 迭代)` 中，哪些部分**可以省略**？（选择所有正确项）

A. 初始化部分（`for (; 条件; 迭代)`）
B. 条件部分（`for (初始化;; 迭代)`，此时循环条件默认为 `true`）
C. 迭代部分（`for (初始化; 条件;)`）
D. 三段式中的两个分号 `;`（即 `for` 括号内无分号）

