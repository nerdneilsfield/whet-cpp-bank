---
qid: cpp-cont-018
type: multi
kp: [cpp-containers]
difficulty: easy
answer_key: [A, C]
---

以下关于 `std::list` 的说法，哪些是正确的？（多选）

A. 在已知迭代器位置插入元素的复杂度为 O(1)
B. 支持 `operator[]` 随机访问
C. 在 `list` 上插入元素不会导致任何迭代器失效
D. `list` 底层使用连续内存存储元素
