---
qid: cpp-cont-006
type: fill
kp: [cpp-containers]
difficulty: easy
answer_key: 唯一且有序（按升序排列）
---

`std::set` 中的元素满足两个特性：元素值 `______`，因此常用于去重并保持有序。

## Explanation

应填 `唯一且有序（按升序排列）`。容器题的核心是底层结构、复杂度和迭代器失效规则。 这个答案对应题干要求的标准名称、成员函数或复杂度写法。常见误区是填写相近但语义不同的接口，或漏掉作用域、大小写、下划线等精确拼写要求。 进一步判断时要回到题干给出的具体代码、接口名和标准语义，避免用相似概念的经验结论替代本题要求。
