---
qid: cpp-cf-008
type: single
kp: [cpp-control-flow]
difficulty: easy
answer_key: A
---

范围 `for` 循环的正确语法是哪一项？

A. `for (int x : vec) { ... }`
B. `for (int x in vec) { ... }`
C. `for (x : int vec) { ... }`
D. `foreach (int x : vec) { ... }`
