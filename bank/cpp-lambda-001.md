---
qid: cpp-lambda-001
type: single
kp: [cpp-lambdas]
difficulty: easy
answer_key: C
---

# Lambda 基本语法结构

下列哪个选项是合法的 C++ lambda 表达式？

A. `[](int x) => x * 2`
B. `lambda(int x) { return x * 2; }`
C. `[](int x) { return x * 2; }`
D. `(int x) -> int { return x * 2; }`
