---
qid: cpp-operators-m-006
type: multi
kp: [cpp-operators]
difficulty: easy
answer_key: [A, C]
---

关于 C++ 中 `%`（取模）运算符，下列哪些说法正确？选择所有正确项。

A. `%` 只能用于整数类型操作数，不能直接用于 `float` 或 `double`。
B. `-7 % 3` 的结果在 C++11 及以后标准中是 `2`。
C. `-7 % 3` 的结果在 C++11 及以后标准中是 `-1`（结果符号与被除数一致）。
D. `7 % 0` 是合法操作，结果为 `0`。
