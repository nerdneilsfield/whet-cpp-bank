---
qid: cpp-move-008
type: single
kp: [cpp-move-semantics]
difficulty: easy
answer_key: A
---

# std::forward 的用途

`std::forward<T>(arg)` 的主要用途是？

A. 在模板函数中将参数以原始值类别（lvalue/rvalue）转发给另一个函数
B. 将参数强制转换为右值引用
C. 将参数复制并传递给另一个函数
D. 检查参数是否为右值
