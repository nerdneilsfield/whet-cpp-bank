---
qid: cpp-iter-019
type: multi
kp: [cpp-iterators]
difficulty: easy
answer_key: [B, C]
---

向 `std::vector` 中间位置 `insert` 一个元素后，以下哪些说法正确？（多选）

A. 所有已有迭代器仍然有效
B. 插入点及其之后位置的迭代器全部失效
C. 若发生内存重新分配，所有迭代器全部失效
D. 只有指向插入点的那个迭代器失效
