---
qid: cpp-types-020
type: fill
kp: [cpp-types]
difficulty: easy
answer_key: "unsigned int"
---
只存储非负整数、与 `int` 位宽相同的无符号类型完整写法是：___

## Explanation

填入 `unsigned int`，因为题目要求的是该规则下的精确表达或标准名称。类型题要关注值类别、cv 限定、隐式转换和推导规则；常见错误是忽略顶层/底层 const 或数组/函数退化。 常见误区是写出近似说法、旧式写法或省略必要的命名空间/符号，导致与答案含义不完全一致。
