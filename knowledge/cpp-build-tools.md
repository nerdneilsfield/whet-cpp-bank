---
kp: cpp-build-tools
title: Build Tools and Toolchain
---

gcc/clang compilation stages, common flags (-O, -g, -Wall, -std, -I, -L, -l), Makefile syntax (targets, prerequisites, recipes, variables, phony), CMake basics (cmake_minimum_required, project, add_executable, target_link_libraries, find_package), static vs shared libraries (.a/.so/.lib/.dll), header vs library, linking vs compiling.
