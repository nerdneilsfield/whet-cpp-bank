#pragma once
#include <vector>
#include <deque>

inline std::vector<int> maxSlidingWindow(const std::vector<int>& a, int k) {
    // TODO: implement using deque
    (void)a; (void)k;
    return {};
}
