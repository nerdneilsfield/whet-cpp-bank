#pragma once
#include <map>
#include <string>
#include <vector>
#include <utility>

inline std::map<std::string, std::vector<int>> groupBy(const std::vector<std::pair<std::string,int>>& pairs) {
    // TODO: implement
    (void)pairs;
    return {};
}
